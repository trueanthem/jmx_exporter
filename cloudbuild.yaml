# See <https://cloud.google.com/cloud-build/docs/build-config> for a reference.

steps:
- name: gcr.io/cloud-builders/docker
  args:
  - build
  - -t
  - us.gcr.io/$PROJECT_ID/jmx_exporter:0.12.0
  - .

images:
- us.gcr.io/$PROJECT_ID/jmx_exporter:0.12.0
